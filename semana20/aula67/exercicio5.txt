exports.handler = async (event) => {
    if (!event.name || !event.cartoon || !event.link) {
           return {
               statusCode: 400,
               body: JSON.stringify({message: "Preencha todos os campos"})
           }
    }

    return {
        statusCode: 200,
        body: JSON.stringify({message: "Personagem cadastrado com sucesso."})
    }
}